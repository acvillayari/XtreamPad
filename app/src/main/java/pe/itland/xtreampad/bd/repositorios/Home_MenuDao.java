package pe.itland.xtreampad.bd.repositorios;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import pe.itland.xtreampad.bd.modelo.Home_Menu;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "Home_Menu".
*/
public class Home_MenuDao extends AbstractDao<Home_Menu, Long> {

    public static final String TABLENAME = "Home_Menu";

    /**
     * Properties of entity Home_Menu.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Home_menu_id = new Property(0, Long.class, "home_menu_id", true, "home_menu_id");
        public final static Property Home_principal_id = new Property(1, Long.class, "home_principal_id", false, "home_principal_id");
        public final static Property Name = new Property(2, String.class, "name", false, "name");
        public final static Property Image_path = new Property(3, String.class, "image_path", false, "image_path");
        public final static Property Deleted = new Property(4, boolean.class, "deleted", false, "deleted");
    }


    public Home_MenuDao(DaoConfig config) {
        super(config);
    }
    
    public Home_MenuDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"Home_Menu\" (" + //
                "\"home_menu_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: home_menu_id
                "\"home_principal_id\" INTEGER," + // 1: home_principal_id
                "\"name\" TEXT," + // 2: name
                "\"image_path\" TEXT," + // 3: image_path
                "\"deleted\" INTEGER NOT NULL );"); // 4: deleted
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"Home_Menu\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Home_Menu entity) {
        stmt.clearBindings();
 
        Long home_menu_id = entity.getHome_menu_id();
        if (home_menu_id != null) {
            stmt.bindLong(1, home_menu_id);
        }
 
        Long home_principal_id = entity.getHome_principal_id();
        if (home_principal_id != null) {
            stmt.bindLong(2, home_principal_id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(3, name);
        }
 
        String image_path = entity.getImage_path();
        if (image_path != null) {
            stmt.bindString(4, image_path);
        }
        stmt.bindLong(5, entity.getDeleted() ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Home_Menu entity) {
        stmt.clearBindings();
 
        Long home_menu_id = entity.getHome_menu_id();
        if (home_menu_id != null) {
            stmt.bindLong(1, home_menu_id);
        }
 
        Long home_principal_id = entity.getHome_principal_id();
        if (home_principal_id != null) {
            stmt.bindLong(2, home_principal_id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(3, name);
        }
 
        String image_path = entity.getImage_path();
        if (image_path != null) {
            stmt.bindString(4, image_path);
        }
        stmt.bindLong(5, entity.getDeleted() ? 1L: 0L);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Home_Menu readEntity(Cursor cursor, int offset) {
        Home_Menu entity = new Home_Menu( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // home_menu_id
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // home_principal_id
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // name
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // image_path
            cursor.getShort(offset + 4) != 0 // deleted
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Home_Menu entity, int offset) {
        entity.setHome_menu_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setHome_principal_id(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setName(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setImage_path(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setDeleted(cursor.getShort(offset + 4) != 0);
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Home_Menu entity, long rowId) {
        entity.setHome_menu_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Home_Menu entity) {
        if(entity != null) {
            return entity.getHome_menu_id();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Home_Menu entity) {
        return entity.getHome_menu_id() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
